<ul class="friend-wrapper">
  <% if (friends != undefined ) { %>
    <% friends.forEach(function(friend){ %>
      <%- include('friend', {friend: friend, onlineUsers: onlineUsers, usersContacted: usersContacted}); %>
    <% }); %>
  <% } %>
</ul>